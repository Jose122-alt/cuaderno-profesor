import 'package:flutter/material.dart';

class AttendanceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Asistencia del Profesor'),
      ),
      body: Center(
        child: Text('Contenido de la pantalla de Asistencia'),
      ),
    );
  }
}