import 'package:flutter/material.dart';

class EvidenceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Evidencia del Profesor'),
      ),
      body: Center(
        child: Text('Contenido de la pantalla de Evidencia'),
      ),
    );
  }
}